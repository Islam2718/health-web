import type { Metadata } from "next";
import { Inter, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";

// Digital Health Apps & Websites guideline calls for Proxima Nova / Frutiger /
// Helvetica Neue; Inter is the free, self-hostable substitute with near
// identical metrics. See docs/typography.md.
const inter = Inter({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ibnocare | Smart Health Care, Connected",
  description:
    "Ibnocare unifies patients, doctors, hospitals, diagnostic centres, ambulance services, and pharmacies onto one secure healthcare platform.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col" suppressHydrationWarning>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
